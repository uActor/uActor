linter:
  image: python:3-alpine
  script: pip install cpplint && cpplint --recursive --root=. --filter -legal,-build/c++11,-whitespace/braces,-build/include_order $(find main_linux uActor main -not -path 'main_linux/build/*' -and -name '*.*pp')